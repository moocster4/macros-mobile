# iOS Simulator can reach the host machine's localhost directly.
# For a physical device via Expo Go, use your machine's LAN IP instead, e.g. http://192.168.1.23:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
